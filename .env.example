# Local dev only. Copy to .env and override as needed.
# Ports are deconflicted against the other WordPress projects in ~/SourceCode.

WP_PORT=9090
PMA_PORT=9091
MAILPIT_PORT=9025
DB_PASSWORD=wordpress
DB_ROOT_PASSWORD=rootpass
